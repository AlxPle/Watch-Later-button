{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Attach: Chrome on :9222",
            "type": "pwa-chrome",
            "request": "attach",
            "port": 9222,
            "webRoot": "${workspaceFolder}"
        }
    ]
}